// -----------------useSelector.js------------
export const loadProductsSelector = (state) => state.products;
export const loadProduct = (id) => {
// Triển khai logic để tải một sản phẩm theo id};
}
// ------------------------------------